import React from 'react';

const propTypes = {};
const defaultProps = {};

export const Application = () => <h1>Hello, world!</h1>;

Application.propTypes = propTypes;
Application.defaultProps = defaultProps;
